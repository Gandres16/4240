#include <stdio.h>

int main()
{
    // declare some variables
    double x = 3.0;
    double y = 0.1;
    double z;

    // Add x and y and store in z
    z = x + y;

    // Print the z to screen
    printf("z = %010.4f\n", z);

    return 0;
}